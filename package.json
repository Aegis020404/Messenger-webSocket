{
  "name": "messenger",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^7.3.0",
    "nodemon": "^2.0.19",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4",
    "ws": "^8.8.1"
  },
  "scripts": {
    "start": "concurrently --kill-others \"nodemon server/websocket.js\" \"react-scripts start\" " ,
    "build": "react-scripts build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
